# Fleet run 2026-09-14 09:14 — 每日天氣通知小工具（12 executors / 8 devices）

- task `f9545be7-9182-4f9f-86d5-32ae1775eff7` · created 2026-09-14 09:14 +0800 · origin `m1-app`
- state **completed** · 12/12 attempts completed with verified receipts · 8 devices: acer, android, ayaneo, ipad-pro, iphone, m1, m5, z13
- planner: `agent` / `groq-free` · chain ['agent:gemini-free', 'agent:groq-free', 'cli:claude', 'cli:opencode', 'cli:codex', 'cli:agy', 'agent:master']

## Prompt（操作者輸入的唯一一句）

> 幫我設計一個用 Python 寫的每日天氣通知小工具：抓取資料、排程、通知方式、錯誤處理、測試。每人只做自己那一份，三句以內。

## 每台機器、每個 AI CLI 的子任務與輸出

### `acer/claude` — completed ✓ receipt verified
- device `acer` · delivery `push` · receipt `cli-process` · local process on acer
- **子任務**：根據需求撰寫工具的總體設計說明，列出模組劃分與流程圖，控制在三句內。

> 本工具採三層模組劃分：輸入解析層（CLI 參數與設定檔讀取）、核心處理層（需求驗證、任務排程、業務邏輯執行）、輸出層（結果格式化、日誌與錯誤回報）。
> 
> 流程圖：使用者輸入 → 參數解析與驗證 → 載入設定 → 核心處理（分派任務 → 逐項執行 → 收集結果）→ 格式化輸出 → 寫入日誌/回傳結束碼。
> 
> 各層以介面解耦，核心層不依賴具體 I/O 實作，方便單元測試與後續擴充新輸入來源或輸出格式。

### `android/app-local-groq` — completed ✓ receipt verified
- device `android` · delivery `pull` · receipt `app-local` · remote-inference-via-groq
- **子任務**：以中文撰寫使用者說明文件，說明安裝與運行方式，三句以內。

> 將專案檔案複製至本地目標目錄。
> 執行安裝指令以配置依賴環境。
> 運行主程式啟動服務。

### `ayaneo/agy` — completed ✓ receipt verified
- device `ayaneo` · delivery `push` · receipt `cli-process` · local process on ayaneo
- **子任務**：實作資料抓取模組，使用 requests 從公開天氣 API 取得 JSON，並返回字典，三句內。

> ```python
> import requestsdef fetch_weather(api_url: str) -> dict:
>     response = requests.get(api_url, timeout=10)
>     response.raise_for_status()
>     return response.json()
> ```使用 `requests.get()` 向公開天氣 API 發送 HTTP GET 請求以取得即時氣象資料。透過 `response.raise_for_status()` 驗證請求成功，最後以 `response.json()` 將回傳的 JSON 資料解析為 Python 字典並返回。334dffaf3c5242038098c6a5491917bf

### `ayaneo/claude` — completed ✓ receipt verified
- device `ayaneo` · delivery `push` · receipt `cli-process` · local process on ayaneo
- **子任務**：為資料抓取模組編寫單元測試，模擬成功與失敗回應，三句內。

> 使用 pytest 搭配 `unittest.mock.patch` 模擬 `requests.get`，成功案例讓 mock 回傳 `status_code=200` 且 `.json()` 回傳預期字典，斷言模組解析出正確資料；失敗案例讓 mock 回傳 `status_code=500` 或直接 `side_effect=requests.ConnectionError`，斷言模組拋出自訂例外或回傳 `None`。每個測試獨立建立 fixture 並用 `assert_called_once_with` 驗證請求 URL 與參數，確保不觸發真實網路。

### `ipad-pro/app-local-groq` — completed ✓ receipt verified
- device `ipad-pro` · delivery `pull` · receipt `app-local` · remote-inference-via-groq
- **子任務**：撰寫通知方式的設計方案，列出可用的 Email 與行動通知選項，三句內。

> 採用推銷者設計的 Email 模板，支援 HTML 與純文字版本以確保跨裝置相容性。
> 行動端整合 APNs 與 FCM 推送服務，並允許使用者自訂通知頻率與類別。
> 系統將依據事件緊急度動態選擇最佳發送管道，同時記錄送出狀態以便後續分析與重試。

### `iphone/app-local-groq` — completed ✓ receipt verified
- device `iphone` · delivery `pull` · receipt `app-local` · remote-inference-via-groq
- **子任務**：編寫中文的錯誤處理策略文檔，說明各類例外的處理方式與日誌記錄，三句內。

> 1. 捕捉並處理已知業務異常，記錄警告級日誌含關鍵上下文（如用戶ID、操作類型）以便追蹤。
> 2. 系統級未預見異常統一走捕獲，記錄錯誤級詳細堆棷與輸入參數，觸發告警通知且返回通用錯誤訊息。
> 3. 敏感數據在日誌中必須脫敏處理，確保合規性並避免洩露個人隱私或機密資訊。

### `m1/agy` — completed ✓ receipt verified
- device `m1` · delivery `push` · receipt `cli-process` · local process on m1
- **子任務**：實作排程功能，使用 schedule 或 APScheduler 每天固定時間觸發抓取與通知，三句內。

> 使用 `APScheduler` 可透過 `scheduler.add_job(crawl_and_notify, 'cron', hour=9, minute=0)` 設定每日固定時間觸發抓取與通知。
> 若使用輕量的 `schedule` 函式庫，則能以 `schedule.every().day.at("09:00").do(crawl_and_notify)` 搭配迴圈輪詢來執行。
> 生產環境建議優先選用 `APScheduler` 並配合守護行程管理，以確保排程長期穩定運作。

### `m1/claude` — completed ✓ receipt verified
- device `m1` · delivery `push` · receipt `cli-process` · local process on m1
- **子任務**：為排程模組寫集成測試，驗證每日任務能正確執行，三句內。

> 為排程模組寫集成測試時，建議使用可控時鐘（假時間或時間注入）觸發排程器，斷言每日任務在預期時間被呼叫且僅執行一次，並驗證執行結果正確寫入資料庫或狀態存儲。同時應搭配測試用的資料庫或mock儲存來檢查任務完成後的副作用（如記錄、通知）是否符合預期。另外別忘了測試邊界情境，例如服務重啟後排程恢復、任務執行失敗重試、以及重疊觸發時的冪等性。

### `m5/agy` — completed ✓ receipt verified
- device `m5` · delivery `push` · receipt `cli-process` · local process on m5
- **子任務**：實作通知模組，支援 SMTP 發送 Email，並返回發送結果，三句內。

> 通知模組可利用 Python 的 `smtplib` 與 `email.message.EmailMessage` 封裝郵件內容與伺服器連線設定。
> 透過 `SMTP_SSL` 或 `STARTTLS` 進行身分驗證並發送郵件，並以 `try-except` 捕捉傳輸過程中的異常。
> 發送結束後統一回傳包含成功狀態（`success: bool`）及回應或錯誤訊息的結果物件。

### `m5/claude` — completed ✓ receipt verified
- device `m5` · delivery `push` · receipt `cli-process` · local process on m5
- **子任務**：為通知模組編寫單元測試，覆蓋成功與失敗情況，三句內。

> 為通知模組編寫單元測試時，先用 mock 取代外部發送管道（如 email/SMS/push client），成功情況驗證 `send()` 回傳成功狀態、mock 被以正確參數呼叫一次、且訊息內容與收件人正確。失敗情況則讓 mock 拋出網路錯誤或回傳失敗碼，驗證模組會正確捕捉例外、回傳失敗狀態或拋出預期的自訂錯誤、記錄日誌，並在有重試機制時驗證重試次數與最終放棄行為。另外補上邊界測試，例如空收件人、無效模板、超長訊息等輸入應被拒絕且不會觸發實際發送。

### `z13/agy` — completed ✓ receipt verified
- device `z13` · delivery `push` · receipt `cli-process` · local process on z13
- **子任務**：設計並編寫錯誤日誌記錄程式，使用 logging 模組寫入檔案與控制台，三句內。

> 使用 Python 的 `logging` 模組，透過 `logging.basicConfig` 配置 `FileHandler` 與 `StreamHandler` 即可同時將錯誤日誌輸出至檔案與控制台。```python
> import logginglogging.basicConfig(
>     level=logging.ERROR,
>     format="%(asctime)s [%(levelname)s] %(message)s",
>     handlers=[
>         logging.FileHandler("error.log", encoding="utf-8"),
>         logging.StreamHandler(),
>     ],
> )
> logging.error("這是一條錯誤日誌訊息")
> ```設定完成後，呼叫 `logging.error()` 即可同時在 `error.log` 檔案與控制台上記錄錯誤資訊。

### `z13/claude` — completed ✓ receipt verified
- device `z13` · delivery `push` · receipt `cli-process` · local process on z13
- **子任務**：配置 CI 工作流，執行 pytest 並生成測試報告，確保代碼質量，三句內。

> 在 GitHub Actions 中新增 `.github/workflows/ci.yml`，於 push/PR 觸發時安裝 Python 與依賴套件，並執行 `pytest --junitxml=report.xml --cov` 產生測試與覆蓋率報告。可搭配 `actions/upload-artifact` 上傳報告，並設定覆蓋率門檻或 lint 步驟（如 flake8/ruff）來確保代碼質量。建議加上分支保護規則，要求 CI 通過才能合併，以持續維持程式碼品質。
